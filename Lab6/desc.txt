Backup:
- mysql
- dns ? (if not covered fully by ansible repo)
- ansible repo
- vault password separately (paper in the safe?)
- backup server setup, whatever isn't covered with ansible; backup all backing up setup
 to AWS (not in code, but described in manual)

ssh-keygen -f <filename>

How to get mysqldump without pushing the password to cron?